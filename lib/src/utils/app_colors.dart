import 'package:flutter/material.dart';

class AppColors {
  // Main Backgrounds
  static const Color bgDark = Color(0xFF121212); // Main app background
  static const Color bgCard = Color(0xFF1F1F1F); // Card/panel background

  // Primary Blues
  static const Color bluePrimary = Color(0xFF1A73E8);
  static const Color blueGlow = Color(0xFF8AB4F8);

  // Text Colors
  static const Color textWhite = Color(0xFFFFFFFF); // Primary text
  static const Color textLight = Color(0xFFCCCCCC); // Secondary text
  static const Color textMuted = Color(0xFF999999); // Hints, subtitles

  // Pins & Icons
  static const Color redPin = Color(0xFFDB4437); // Destination pin
  static const Color greenTransit = Color(0xFF34A853); // Metro, bus icons

  // Warnings & States
  static const Color warning = Color(0xFFFFB74D); // Restrictions, alerts
  static const Color inactiveIcon = Color(0xFF5F6368); // Inactive mode icons

  // Optional: Material You / Dynamic variants (if needed)
  static const Color homeIconBlue = Color(0xFF4285F4);
}
